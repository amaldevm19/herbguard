<div class="offline-page">
  <div class="offline-card">
    <div class="offline-icon">
      <i data-lucide="wifi-off"></i>
    </div>
    <h1 class="offline-title">You're Offline</h1>
    <p class="offline-sub">
      HerbGuard can't reach the server right now.
      Check your network connection and try again.
    </p>
    <div class="offline-tips">
      <div class="offline-tip">
        <i data-lucide="wifi"></i>
        <span>Make sure you're connected to the office WiFi</span>
      </div>
      <div class="offline-tip">
        <i data-lucide="server"></i>
        <span>The Raspberry Pi server may be restarting</span>
      </div>
    </div>
    <button class="offline-retry" onclick="window.location.reload()">
      <i data-lucide="refresh-cw"></i>
      Try Again
    </button>
  </div>
</div>

<style>
.offline-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  background: var(--bg);
}

.offline-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-xl);
  padding: 3rem 2rem;
  max-width: 420px;
  width: 100%;
  text-align: center;
}

.offline-icon {
  width: 72px;
  height: 72px;
  background: var(--surface2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1.5rem;
  color: var(--muted);
}

.offline-icon svg { width: 32px; height: 32px; }

.offline-title {
  font-family: var(--font-display);
  font-size: 1.6rem;
  font-weight: 700;
  margin-bottom: 0.75rem;
}

.offline-sub {
  font-size: 0.85rem;
  color: var(--muted);
  line-height: 1.6;
  margin-bottom: 1.5rem;
}

.offline-tips {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-bottom: 2rem;
  text-align: left;
}

.offline-tip {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  background: var(--surface2);
  border-radius: var(--radius-sm);
  padding: 0.75rem 1rem;
  font-size: 0.8rem;
  color: var(--muted);
}

.offline-tip svg { width: 15px; height: 15px; flex-shrink: 0; color: var(--amber); }

.offline-retry {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 2rem;
  background: var(--green-mid);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 0.88rem;
  font-weight: 500;
  font-family: var(--font-sans);
  cursor: pointer;
  transition: background 0.15s;
}

.offline-retry:hover { background: #268f3e; }
.offline-retry svg   { width: 15px; height: 15px; }
</style>